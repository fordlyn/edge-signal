# 页面架构规划 // Page Architecture Spec
> **Project**: NEON_BROADCAST_V1  
> **Status**: Planning  
> **Last Updated**: 2025-12-27

## 1. 架构概览 (Overview)

基于"节点广播"隐喻，系统页面分为四个层级：

| 层级 | 代号 | 访问权限 | 描述 |
|:-----|:-----|:---------|:-----|
| **接入层** | ACCESS | Public | 身份验证相关 |
| **核心层** | CORE | Public | 访客主要交互界面 |
| **控制层** | CONTROL | Operator | 后台管理界面 |
| **异常层** | EXCEPTION | Public | 错误与状态页面 |

## 2. 页面清单 (Page Inventory)

### 2.1 接入层 (ACCESS)

| 页面 | 路由 | 状态 | 描述 |
|:-----|:-----|:-----|:-----|
| 节点接入 | `/login` | ✅ Designed | 系统登录 |
| 节点注册 | `/register` | ⏸️ Deferred | 新用户注册 (MVP 暂缓) |
| 密钥重置 | `/forgot-password` | ⏸️ Deferred | 密码找回流程 |

**设计决策**：MVP 阶段仅支持单操作员模式，注册功能延后实现。

### 2.2 核心层 (CORE)

| 页面 | 路由 | 状态 | 描述 |
|:-----|:-----|:-----|:-----|
| 数据看板 | `/` | ✅ Designed | 首页，文章流 + 系统状态 |
| 档案读取 | `/post/[slug]` | ✅ Designed | 单篇文章阅读 |
| 操作员档案 | `/about` | ✅ Designed | 个人介绍 |
| 信号归档 | `/archive` | 📋 Planned | 按时间线展示所有文章 |
| 频段索引 | `/tags` | 📋 Planned | 标签聚合页 |
| 频段详情 | `/tags/[tag]` | 📋 Planned | 单标签下的文章列表 |
| 分区索引 | `/categories` | 📋 Planned | 分类聚合页 |
| 分区详情 | `/categories/[slug]` | 📋 Planned | 单分类下的文章列表 |
| 信号检索 | `/search` | 📋 Planned | 搜索结果页 |
| 通信链路 | `/contact` | 📋 Planned | 联系表单 |

**新增页面设计要点**：

#### 信号归档 (`/archive`)
- **布局**：时间轴 (Timeline) 样式，按年/月分组
- **交互**：点击年份展开/折叠该年文章
- **视觉**：垂直线 + 节点标记，模拟数据传输日志

#### 频段索引 (`/tags`)
- **布局**：标签云 (Tag Cloud) 或网格
- **交互**：悬停显示该标签文章数
- **视觉**：标签大小/亮度与文章数成正比

#### 信号检索 (`/search`)
- **布局**：顶部搜索框 + 结果列表
- **交互**：实时搜索 / 防抖
- **视觉**：关键词高亮，模拟"信号匹配"效果

#### 通信链路 (`/contact`)
- **布局**：联系表单 + 社交链接
- **交互**：表单验证 + 发送状态反馈
- **视觉**：表单字段使用切角容器风格

### 2.3 控制层 (CONTROL)

> 操作员专属区域，需通过身份验证后访问。

| 页面 | 路由 | 状态 | 描述 |
|:-----|:-----|:-----|:-----|
| 控制中心 | `/dashboard` | 📋 Planned | 数据概览 (访问量、文章数等) |
| 信号编辑器 | `/dashboard/editor` | 📋 Planned | Markdown 编辑器 |
| 信号编辑 | `/dashboard/editor/[id]` | 📋 Planned | 编辑现有文章 |
| 档案管理 | `/dashboard/posts` | 📋 Planned | 文章列表 CRUD |
| 评论审核 | `/dashboard/comments` | 📋 Planned | 评论管理 |
| 系统配置 | `/dashboard/settings` | 📋 Planned | 博客基础设置 |

**设计要点**：

#### 控制中心 (`/dashboard`)
- **数据卡片**：文章总数、总访问量、待审核评论数
- **快捷操作**：新建文章、查看最新评论
- **系统日志**：最近活动流

#### 信号编辑器 (`/dashboard/editor`)
- **核心组件**：分屏 Markdown 编辑器 (左编辑/右预览)
- **元数据面板**：标题、标签、分类、封面图
- **自动保存**：草稿本地持久化
- **发布流程**：预览 → 确认 → 发布

#### 档案管理 (`/dashboard/posts`)
- **列表**：表格形式，支持排序/筛选
- **批量操作**：删除、更改状态
- **状态标识**：已发布 / 草稿 / 已归档

### 2.4 异常层 (EXCEPTION)

| 页面 | 路由 | 状态 | 描述 |
|:-----|:-----|:-----|:-----|
| 信号丢失 | `/404` (not-found) | ✅ Designed | 页面不存在 |
| 访问拒绝 | `/403` | 📋 Planned | 权限不足 |
| 系统故障 | `/500` (error) | 📋 Planned | 服务器错误 |
| 维护模式 | `/maintenance` | 📋 Planned | 系统升级占位页 |

**设计要点**：

#### 访问拒绝 (`/403`)
- **视觉**：警告色调 (--accent-magenta)
- **文案**："链路权限不足 // ACCESS DENIED"
- **动效**：锁定图标 + 扫描失败动画

#### 系统故障 (`/500`)
- **视觉**：严重警告 (红色基调)
- **文案**："节点异常 // NODE MALFUNCTION"
- **动效**：更剧烈的 Glitch 效果

#### 维护模式 (`/maintenance`)
- **视觉**：中性色调 (--accent-amber)
- **文案**："系统升级中 // SYSTEM UPGRADE IN PROGRESS"
- **动效**：进度条或倒计时

## 3. 实现优先级 (Priority Matrix)

### Phase 1: MVP Core
1. `/` - 首页 (已有)
2. `/post/[slug]` - 文章页 (已有)
3. `/about` - 关于页 (已有)
4. `/login` - 登录页 (已有)
5. `/404` - 404 页 (已有)

### Phase 2: Content Discovery
6. `/archive` - 归档页
7. `/tags` - 标签索引
8. `/tags/[tag]` - 标签详情
9. `/search` - 搜索页

### Phase 3: Admin Panel
10. `/dashboard` - 控制中心
11. `/dashboard/posts` - 文章管理
12. `/dashboard/editor` - 编辑器

### Phase 4: Polish
13. `/categories` - 分类系统
14. `/contact` - 联系页
15. `/403`, `/500`, `/maintenance` - 完善异常层
16. `/dashboard/comments`, `/dashboard/settings` - 完善后台

## 4. 路由结构 (Route Tree)

```
app/
├── (public)/                    # 公开路由组
│   ├── page.tsx                 # /
│   ├── about/page.tsx           # /about
│   ├── archive/page.tsx         # /archive
│   ├── search/page.tsx          # /search
│   ├── contact/page.tsx         # /contact
│   ├── post/[slug]/page.tsx     # /post/[slug]
│   ├── tags/
│   │   ├── page.tsx             # /tags
│   │   └── [tag]/page.tsx       # /tags/[tag]
│   └── categories/
│       ├── page.tsx             # /categories
│       └── [slug]/page.tsx      # /categories/[slug]
├── (auth)/                      # 认证路由组
│   ├── login/page.tsx           # /login
│   ├── register/page.tsx        # /register (deferred)
│   └── forgot-password/page.tsx # /forgot-password (deferred)
├── dashboard/                   # 后台路由
│   ├── layout.tsx               # 后台布局 (侧边栏)
│   ├── page.tsx                 # /dashboard
│   ├── posts/page.tsx           # /dashboard/posts
│   ├── editor/
│   │   ├── page.tsx             # /dashboard/editor (新建)
│   │   └── [id]/page.tsx        # /dashboard/editor/[id] (编辑)
│   ├── comments/page.tsx        # /dashboard/comments
│   └── settings/page.tsx        # /dashboard/settings
├── not-found.tsx                # 404
├── error.tsx                    # 500
├── layout.tsx                   # 根布局
└── globals.css                  # 全局样式
```

## 5. 组件复用规划 (Shared Components)

新页面应复用以下组件：

| 组件 | 用途 | 使用页面 |
|:-----|:-----|:---------|
| `Navbar` | 顶部导航 | 所有公开页面 |
| `Footer` | 页脚 | 所有公开页面 |
| `PostCard` | 文章卡片 | 首页、归档、标签、分类、搜索 |
| `TagBadge` | 标签徽章 | 文章页、标签页 |
| `SearchInput` | 搜索输入框 | 导航栏、搜索页 |
| `Sidebar` | 后台侧边栏 | 所有 dashboard 页面 |
| `DataCard` | 数据统计卡片 | dashboard 首页 |
| `MarkdownEditor` | Markdown 编辑器 | 编辑器页面 |
| `ErrorDisplay` | 错误展示 | 403, 500, maintenance |

---

*文档版本: 1.0.0* // *最后更新: 2025-12-27*
